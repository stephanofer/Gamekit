package network.hera.gamekit.arena.allocation;

public enum ArenaAllocationRejectReason {
    NO_COMPATIBLE_ARENA,
    NO_SERVER_AVAILABLE,
    ARENA_ALREADY_RESERVED,
    RESERVATION_FAILED,
    INVALID_REQUIREMENTS
}
